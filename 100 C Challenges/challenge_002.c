#include<stdio.h>
int main(){
    int a,b;
    printf("Give two numbers: ");
    scanf("%d %d",&a,&b); //Taking numbers as input
    int sum = a+b; //calculating sum of nummbers
    printf("\nsum = %d",sum); //Printing sum of numbers
    return 0;
}
